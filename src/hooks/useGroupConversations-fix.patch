--- useGroupConversations.ts.orig
+++ useGroupConversations.ts
@@ -8,7 +8,7 @@
 import { useState, useEffect, useCallback, useMemo } from 'react';
 import { eventEmitter } from '@/lib/event-emitter';
-import { websocketService } from '@/lib/websocket-service';
+import { connectionManager } from '@/lib/connection-manager';
 import type {
   GroupConversation,
   GroupMember,
@@ -237,7 +237,8 @@
     ): Promise<string> {
       try {
         const requestId = crypto.randomUUID();
-        const cid = websocketService.getCurrentCid();
+        const connectionInfo = connectionManager.getConnectionInfo();
+        const cid = connectionInfo?.cid;
 
         if (!cid) {
           throw new Error('Not connected to server');
@@ -276,7 +277,8 @@
   const invitePeer = useCallback(
     async (groupId: string, peerCid: string, roleId?: string): Promise<void> => {
       try {
-        const cid = websocketService.getCurrentCid();
+        const connectionInfo = connectionManager.getConnectionInfo();
+        const cid = connectionInfo?.cid;
         if (!cid) {
           throw new Error('Not connected to server');
         }
@@ -313,7 +315,8 @@
   // Leave a group
   const leaveGroup = useCallback(async (groupId: string): Promise<void> => {
     try {
-      const cid = websocketService.getCurrentCid();
+      const connectionInfo = connectionManager.getConnectionInfo();
+      const cid = connectionInfo?.cid;
       if (!cid) {
         throw new Error('Not connected to server');
       }
@@ -346,7 +349,8 @@
   const kickMember = useCallback(
     async (groupId: string, memberCid: string): Promise<void> => {
       try {
-        const cid = websocketService.getCurrentCid();
+        const connectionInfo = connectionManager.getConnectionInfo();
+        const cid = connectionInfo?.cid;
         if (!cid) {
           throw new Error('Not connected to server');
         }
@@ -414,7 +418,8 @@
   const refresh = useCallback(async (): Promise<void> => {
     try {
       setLoading(true);
-      const cid = websocketService.getCurrentCid();
+      const connectionInfo = connectionManager.getConnectionInfo();
+      const cid = connectionInfo?.cid;
       if (!cid) {
         throw new Error('Not connected to server');
       }
