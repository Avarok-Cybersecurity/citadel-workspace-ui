/**
 * One implementation, in lib/p2p/current-cid. This file's own header used to
 * read "Extracted to avoid duplication across service methods" — while an
 * identical chain lived in p2p-registration-service/discovery.ts.
 */
export { getCurrentCid } from '../p2p/current-cid';
